image:
  repository: tagspaces/tagspaces-lite-web
  pullPolicy: IfNotPresent
  tag: 6.2.1@sha256:9d7ff9069a9fabf0786bc0c191cf9cc323919e236fe34293916cf246a70d667c
securityContext:
  container:
    readOnlyRootFilesystem: false
service:
  main:
    ports:
      main:
        protocol: http
        targetPort: 80
        port: 5000
workload:
  main:
    podSpec:
      containers:
        main:
          env:
            KEY:
              secretKeyRef:
                name: tagspaces-secret
                key: KEY
persistence:
  nginx:
    enabled: true
    type: emptyDir
    mountPath: "/var/cache/nginx"
portal:
  open:
    enabled: true
