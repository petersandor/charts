image:
  repository: tagspaces/tagspaces-lite-web
  pullPolicy: IfNotPresent
  tag: 6.4.5@sha256:836413d5ea9d711e55009b54dd6fe59d98bf81b119e8f7d7685e32c80eae5abf
securityContext:
  container:
    readOnlyRootFilesystem: false
service:
  main:
    ports:
      main:
        protocol: http
        targetPort: 80
        port: 5000
workload:
  main:
    podSpec:
      containers:
        main:
          env:
            KEY:
              secretKeyRef:
                name: tagspaces-secret
                key: KEY
persistence:
  nginx:
    enabled: true
    type: emptyDir
    mountPath: "/var/cache/nginx"
portal:
  open:
    enabled: true
