image:
  repository: tagspaces/tagspaces-lite-web
  pullPolicy: IfNotPresent
  tag: 6.0.4@sha256:e65057962a908163c996f0215d2955a501783f511436c53efc548ba1ce929155
securityContext:
  container:
    readOnlyRootFilesystem: false
service:
  main:
    ports:
      main:
        protocol: http
        targetPort: 80
        port: 5000
workload:
  main:
    podSpec:
      containers:
        main:
          env:
            KEY:
              secretKeyRef:
                name: tagspaces-secret
                key: KEY
persistence:
  nginx:
    enabled: true
    type: emptyDir
    mountPath: "/var/cache/nginx"
portal:
  open:
    enabled: true
