image:
  repository: pepperlabs/peppermint
  pullPolicy: IfNotPresent
  tag: latest@sha256:16f1a6d51eb9e829c72cce0ece36ef60f0c62a26100038f55030a6af6ad8f5fc

securityContext:
  container:
    runAsNonRoot: false
    readOnlyRootFilesystem: false
    runAsUser: 0
    runAsGroup: 0

service:
  main:
    ports:
      main:
        protocol: http
        targetPort: 3000
        port: 10699
  api:
    enabled: true
    type: ClusterIP
    ports:
      api:
        enabled: true
        protocol: http
        port: 5003

workload:
  main:
    podSpec:
      containers:
        main:
          env:
            DB_USERNAME: "{{ .Values.cnpg.main.user }}"
            DB_PASSWORD:
              secretKeyRef:
                name: cnpg-main-user
                key: password
            DB_HOST:
              secretKeyRef:
                name: cnpg-main-urls
                key: host
            SECRET:
              secretKeyRef:
                name: peppermint-secrets
                key: SECRET
            API_URL: '{{ printf "http://%v:%v" (include "tc.v1.common.lib.chart.names.fullname" $) .Values.service.api.ports.api.port }}'

cnpg:
  main:
    enabled: true
    user: peppermint
    database: peppermint

portal:
  open:
    enabled: true
